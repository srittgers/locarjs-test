console.log("Script loaded!");

// Your WebXR initialization code would go here, for example:
// navigator.xr?.requestSession('immersive-vr').then(onSessionStarted);
// function onSessionStarted(session) { /* ... */ }
